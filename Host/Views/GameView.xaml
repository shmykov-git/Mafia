<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:model="clr-namespace:Host.Mafia.ViewModel"             
             x:Class="Host.Views.GameView">

    <ScrollView>
        <VerticalStackLayout 
            Padding="30,0"
            Spacing="25" >

            <Label
                Text="{Binding HostHint}"
                Style="{StaticResource Headline}"
                SemanticProperties.HeadingLevel="Level1" 
                HorizontalOptions="Center" />

            <ListView ItemsSource="{Binding ActivePlayers}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <Grid ColumnDefinitions="0.8*,0.2*" IsEnabled="{Binding IsEnabled}">
                                <Label Grid.Column="0"
                                   Text="{Binding Player}"
                                   HorizontalOptions="Start"
                                   FontAttributes="Bold" />

                                <StackLayout Grid.Column="1" Orientation="Horizontal" HorizontalOptions="End">
                                    <CheckBox IsChecked="{Binding IsSelected}" />
                                </StackLayout>
                            </Grid>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>            
            
            <Button 
                    Text="Continue" 
                    SemanticProperties.Hint="Ready to continue the game"
                    Command="{Binding ContinueCommand}"
                    HorizontalOptions="Fill" />

            <Label
                Text="{Binding Text}"
                HorizontalOptions="Start" />        
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
